import { Meta } from "@/components/templates/meta";
import { Layout } from "@/components/templates/layout";

const AgentFinder = () => {
  return (
    <Layout meta={<Meta title="AgentFinder" description="AgentFinder Page" />}>
      AgentFinder
    </Layout>
  );
};

export default AgentFinder;
