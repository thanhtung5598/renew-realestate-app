import { Meta } from "@/components/templates/meta";
import { Layout } from "@/components/templates/layout";

const Buy = () => {
  return (
    <Layout meta={<Meta title="Buy" description="Buy Page" />}>Buy</Layout>
  );
};

export default Buy;
