import { Meta } from "@/components/templates/meta";
import { Layout } from "@/components/templates/layout";

const Register = () => (
  <Layout meta={<Meta title="Register" description="Where you can register" />}>
    <div>Register</div>
  </Layout>
);

export default Register;
