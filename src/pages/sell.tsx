import { Meta } from "@/components/templates/meta";
import { Layout } from "@/components/templates/layout";

const Sell = () => {
  return (
    <Layout meta={<Meta title="Sell" description="Sell Page" />}>Sell</Layout>
  );
};

export default Sell;
